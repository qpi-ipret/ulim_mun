<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Easy Puzzle</title>
  <style>
    body {
      font-family: 'Noto Sans KR', sans-serif;
      background-color: #eef2f7;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .container {
      background-color: #fff;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 2px 2px 10px rgba(0,0,0,0.1);
      text-align: center;
      width: 300px;
    }
    input {
      padding: 10px;
      font-size: 16px;
      width: 100%;
      margin-bottom: 15px;
      border-radius: 8px;
      border: 1px solid #ccc;
    }
    button {
      padding: 10px;
      font-size: 16px;
      background-color: #4CAF50;
      color: white;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      width: 100%;
    }
    #message {
      margin-top: 15px;
      font-weight: bold;
    }
    .success {
      color: green;
    }
    .fail {
      color: red;
    }
  </style>
</head>
<body>
  <div id="hidden" data-answer="rabbit"></div> <!-- 정답이 여기에 숨어있음 -->

  <div class="container">
    <h2>정답을 입력하세요</h2>
    <input type="text" id="inputBox" placeholder="힌트는 어디에 있을까?" />
    <button onclick="checkAnswer()">제출</button>
    <p id="message"></p>
  </div>

  <script>
    function checkAnswer() {
      const input = document.getElementById('inputBox').value.trim();
      const correct = document.getElementById('hidden').dataset.answer;
      const message = document.getElementById('message');

      if (input === correct) {
        message.textContent = "🎉 정답입니다!";
        message.className = "success";
      } else {
        message.textContent = "❌ 틀렸습니다.";
        message.className = "fail";
      }
    }
  </script>
</body>
</html>
